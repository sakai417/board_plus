
<h1>My Threads</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Overview</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @my_threads.each do |my_thread| %>
      <tr>
        <td><%= my_thread.title %></td>
        <td><%= my_thread.overview %></td>
        <!-- my_thread_pathとせずにmy_threadと変数を渡すだけで良い。 -->
        <!-- my_thread_path(my_thread)と丁寧に描いても良い。 -->
        <td><%= link_to 'Show', my_thread_path(my_thread) %></td>
        <td><%= link_to 'Edit', edit_my_thread_path(my_thread) %></td>
        <td><%= link_to 'Delete', my_thread, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New My Thread', new_my_thread_path %>
